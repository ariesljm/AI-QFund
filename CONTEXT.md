# AI-QFund 领域词汇表（2.0）

中国公募基金量化投研终端：数据基座 → 全市场初筛（Top30 候选池）→ LLM 排雷审计 → 三级状态跟踪 → 校准/知识回流 的日循环。
每日自动跑数据、产出"本周推荐 Top5"的决策，并持续审计该决策是否正确、把教训回流到未来的决策。

> 2.0 词汇变更（票 24）：**赛道 / 防线 / R1–R5 / 裁决损耗 已作废**（失去指称对象）。
> **赛道降级为标签口径**：同类 = RBSA 第一行业（n≥10），仅用于超额收益基准，不再决定推荐范围。
> 推荐从"选赛道 → 赛道内排序"改为"全市场初筛 → LLM 排雷 → Top5"。

## 数据基座

**数据基座**（Data Foundation）:
每天自动把基金列表、净值、指数、重仓股、行业映射、个股估值/日线、特征 拉取/计算/落库的管道步骤集合。
_Avoid_: 数据抓取、ETL

**可重建表**：
数据基座可随时重跑产出的表（fund_basic / fund_nav / index_daily / fund_holdings / stock_industry_map / fund_features / stock_valuation_daily / stock_daily），写归 data/store。
_Avoid_: 原始数据

**决策域表**：
承载用户/推荐/监控生命周期数据的表（推荐记录、结算账本、监控事件、质量度量、审计记录、空推荐记录等），写归 repo/decision。
_Avoid_: 业务表

**净值时间状态**：
每基金净值日期区间 + 全部净值日期集的聚合查询（打标/陈旧/增量下载共用单一归属）。
_Avoid_: 净值查询

**PIT 公告日**（disclosure_date）:
持仓可见性闸门（ADR-0010）：`fund_holdings.disclosure_date <= d` 才视为决策日 d 的可见持仓；东财无公告日字段，一律保守推算为报告期 + 15 个工作日（宁晚不早）。
_Avoid_: 报告期口径（会泄漏污染回测）

## 模块一：全市场初筛

**主动权益池**：
混合型 + 股票型基金（剔指数型）；硬过滤链（合并规模 <5000 万 或 >100 亿 / 暂停申购 / 单日上限 <1000 元 / 净值历史 <62 条）剔除后进入打分。
_Avoid_: 全基金

**Top30 候选池**：
LightGBM 打分排序的全市场 Top30；落库并保留特征快照（供审计与复盘）。2.0 无"赛道无候选 → 全市场 Top10"降级路径。
_Avoid_: 候选池（1.x 语义）

**空推荐日**：
区分 `no_opportunity`（市场判断无机会）与 `data_failure`（数据基座故障导致无法产出）两类合法空日，记录并可回溯。
_Avoid_: 无推荐、异常

## 模块二：LLM 排雷审计

**审计 JSON**（audit_verdict / risk_score / veto_reasons / audit_details / recommendation_summary）:
LLM 对候选基金的结构化排雷输出；schema 严格校验（非法即失败，不静默降级）；`VETO` 或 `risk_score > 60` 剪枝，复合分 `LGBM × (1 − Risk/100)` 定 Top5。
_Avoid_: 自由文本意见

**审计失效条件三元组**：
审计缓存的失效判据 = 持仓期次 / 重仓股集合指纹 / 风险事件版本；任一变化才重跑 LLM（事件驱动而非时钟驱动）。

## 模块三：跟踪与校准

**跟踪对象**（Tracked Object）:
三级状态机（HOLD / WATCH / EXIT，ADR-0011）作用的对象，默认实现为推荐记录；`object_type/object_id` 留缝供将来接入用户持仓（加表不改状态机）。
_Avoid_: 防线、持仓（1.x 语义）

**信号**：
状态机转移的触发条件（脱轨 |corr|<0.40 或 r2<0.25 / 估值分位 ≥85% / Alpha 连续 5 日负 → WATCH；跌破 EMA20 / 估值 >90% 且动量转负 / 致命公告 → EXIT）。EXIT 不可逆。
_Avoid_: 防线动作（EXIT > WARNING > BUY_MORE > HOLD 已作废）

**净值陈旧告警**：
基金净值停更时记录的数据告警事件——属数据问题，**不计入**状态升级序列（数据问题 ≠ 信号）。
_Avoid_: 信号

**校准层**（Calibration）:
每路信号如实记账自己的历史命中率与样本量（票 18）；连续失灵 3 次降权 / 5 次停用；样本不足不降权。命中率曲线 = 概念漂移的可观测数字。
_Avoid_: 调参（校准是诚实记账，不需要优化器）

**规则退役**（票 20）:
否决类规则单向增严不可逆（ADR-0009）；信号/特征类命中率长期低于阈值且样本足够 → 提案退役（留痕可恢复），系统因此**能变简单**。

## 自我进化

**知识库**（Bad/Good-Case）:
复盘归因 → Few-Shot 回流（票 17）。异常回撤 >8% 或触发 EXIT 时归因分叉：量化特征滞后（记特征失效）vs 非结构化隐患漏判（生成标准案例）；案例结构化可检索、只增不改，活跃案例注入排雷 prompt。
_Avoid_: 洞察（1.x 的 evolution_insights，消费者已作废）

**影子闸门**（Champion-Challenger）:
C 层模型自动切换判据（票 19）：≥3 个不重叠子区间（牛/熊/震荡）同向不劣 + 合并区间 IR 提升通过非重叠窗口显著性检验；同一主标尺版本下比较；上线后自动回滚。
_Avoid_: IR 高 15% 阈值（±8pp 噪声下不可辨识）

## 质量与回测

**主标尺**（超额收益，BENCHMARK_VERSION）:
推荐后 FORWARD_DAYS（40）交易日的收益 − 同类（RBSA 第一行业、n≥10、不含自身）同期均值。主标尺口径变更 = 换版本号 + 人工批准（ADR-0008）。
_Avoid_: 绝对收益（混入市场 beta 与赛道景气）

**用户口径**（绝对胜率）:
绝对收益 > 1%（覆盖申赎成本）的占比；仅 Web 展示与用户沟通用，**不参与**排序/训练/结算聚合（ADR-0008 三标尺分职）。
_Avoid_: 胜率

**标签**（y_excess / y_abs）:
训练目标 = 同类中性化超额收益 − λ×40 日最大回撤（λ 在 settings.toml [label]，初值 1.0）；y_abs 绝对收益并列保留（用户口径可比性）。LABEL_VERSION 强校验：不一致强制重训。
_Avoid_: 纯绝对收益标签（1.x）

**素材装配**：
把结构化数据（基金画像 / PIT 可见重仓股 / 事件切片）转成 LLM 可读中文段落的单一归属（llm/context，ADR-0003）。
_Avoid_: 提示词拼接（仅指文案格式化部分）

**面板采样**（panel_samples）：
生产训练与研究回测共用的样本构建深函数：净值 → 预热 → PIT 可见持仓 → 特征现算 → 标签集（超额 y_excess + 绝对 y_abs + λ 风险调整）。
_Avoid_: 采样循环拷贝

**统计原语**（features/stats）：
features 域内的秩相关/线性相关/显著性检验命名函数（内部 scipy，ADR-0007 豁免边界内）；校准层/影子闸门消费，引擎层不直接 import scipy。
_Avoid_: scipy 直连

**期望交易日**：
今日在交易日历内 → 昨交易日（盘前拉 T-1），否则最近交易日；收敛于 trading_calendar.expected_trade_date，指数/特征新鲜度共用。
_Avoid_: 期望日期（多实现）