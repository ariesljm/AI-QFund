# AI-QFund 领域上下文

AI 驱动的基金量化投研系统：数据基座拉取市场数据 → 推荐引擎生成投资建议 → 监控引擎盯盘 → 进化引擎沉淀教训。

## Language

**推荐决策域**:
推荐引擎产出的决策数据集合，包含推荐记录、赛道选择、监控事件、进化洞察与推荐结果文件。清除时作为一个整体删除。
_Avoid_: 推荐数据、推荐历史

**底层数据**:
由数据基座从外部 API 拉取或计算的原始数据（基金列表、净值、指数、持仓、特征、行业映射）。清除推荐决策域时保留，管线可自动重建。
_Avoid_: 基础数据、缓存

**推荐**:
`recommend_log` 中的记录，含当前持仓（HOLD）与历史（EXIT）。同一基金允许被多次推荐（多行记录并存）；推荐次数在监控面板展示，累计涨跌幅始终按首次推荐的入场净值计算。

**推荐周期**:
一次推荐对应的预期持有/监控窗口。产品层定义为 30 天；买入信号由量化模型以更短的预测窗口（20 交易日）产生，持仓在监控引擎发出离场信号前持续跟踪，不受 30 天硬性截止约束。

**已推荐历史**:
`recommend_log` 中状态为 EXIT 的已平仓/已否决记录。

**空推荐日**:
系统（LLM 宏观分析）判断当天没有合适机会时的合法业务结果：管线正常结束并记录"今日无推荐"，区别于 LLM 调用/解析失败这类技术异常。

**信号**:
监控引擎对持仓基金每日产出的状态判定：持有(HOLD)/加仓(BUY_MORE)/警惕(WARNING)/离场(EXIT)。入库 `monitor_events` 并通过 Web 面板展示给用户，不主动推送。

**自我进化**:
系统持续提升推荐质量的机制集合：从历史结果沉淀教训（LLM 洞察）回流决策、自动调整排序权重，并通过量化指标（预测表现与实际表现的相关性、胜率趋势）度量推荐质量并反哺参数与洞察采纳。进化按月触发。

**推荐质量**:
对推荐结果有效性的量化度量，包括：信息系数（模型预测分与未来20日实际超额收益的秩相关）、超额胜率（20日窗口跑赢沪深300的推荐占比）、累计超额曲线。

**管线**:
数据基座 → 推荐 → 监控 → 进化（每月）的全流程编排。
