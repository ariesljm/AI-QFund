# AI-QFund 领域词汇表

中国公募基金量化投研终端：数据基座 → 推荐引擎 → 追踪监控 → 自我进化 的日循环。
每日自动跑数据、产出"今天买什么"的决策，并持续审计该决策是否正确、把教训回流到未来的决策。

## 数据基座

**数据基座**（Data Foundation）:
每天自动把基金列表、净值、指数、重仓股、行业映射、特征 拉取/计算/落库的管道步骤集合。
_Avoid_: 数据抓取、ETL

**可重建表**：
数据基座可随时重跑产出的表（fund_basic / fund_nav / index_daily / fund_holdings / stock_industry_map / fund_features），写归 data/store。
_Avoid_: 原始数据

**决策域表**：
承载用户/推荐/监控生命周期数据的表（推荐记录、赛道选择、监控事件、进化洞察、质量度量等），写归 repo/decision。
_Avoid_: 业务表

**净值时间状态**：
每基金净值日期区间 + 全部净值日期集的聚合查询（打标/陈旧/增量下载共用单一归属）。
_Avoid_: 净值查询

## 推荐引擎

**赛道**（Sector):
量化定池产出的可投行业（RBSA 第一行业），LLM 在池内选择/否决。
_Avoid_: 板块、概念

**候选池**：
量化信号（动量/热度）筛出的本日可投赛道集合；LLM 只在池内选择，池外推荐视为无效。
_Avoid_: 大盘

**空推荐日**：
LLM 显式判定"今日无合适赛道"或量化池无候选的合法决策日，记录并可回溯。
_Avoid_: 无推荐、异常

## 追踪监控

**防线**（Defense Rule):
监控引擎对每只持仓运行的规则（R1 趋势退出 / 风格漂移 / 赛道锚点 / 赛道优势 / 模型信号 / LLM 逻辑证伪）。
_Avoid_: 规则、检查项

**信号**：
防线输出的动作语义，优先级 EXIT > WARNING > BUY_MORE > HOLD。
_Avoid_: 状态

**净值陈旧告警**：
基金净值停更（停牌/数据断裂）时记录的数据告警事件——属数据问题，不计入 WARNING 升级序列。
_Avoid_: 信号

## 自我进化

**进化引擎**（Evolve):
月度结算 → 批量 LLM 元分析 → 教训入库 → 回流到选赛道/定论 prompt 的闭环。
_Avoid_: 自学习、调参

**洞察**（Insight):
元分析产出的历史教训条目，带置信度（胜案例 +0.10、月度 ×0.95 衰减），活跃洞察进 LLM prompt。
_Avoid_: 规则、经验

**裁决损耗**：
LLM 选中基金 vs 候选池均值的 40 日收益差——回答"LLM 是否不如纯量化"。
_Avoid_: 选股误差

## 质量与回测

**40 日收益**：
推荐后 FORWARD_DAYS（40）交易日的绝对收益——结算、质量度量、回测、训练样本共用的主标尺。
_Avoid_: 收益率、涨幅

**赚钱胜率**：
绝对收益 > 1%（覆盖申赎成本）的占比，进化评估的主指标。
_Avoid_: 胜率

**素材装配**：
把结构化数据（持仓/锚点/RBSA 分布/候选池/技术面）转成 LLM 可读中文段落的单一归属（llm/context）。
_Avoid_: 提示词拼接（仅指文案格式化部分）
**板块收益矩阵**（style_returns_matrix）：
把板块日涨幅（pct 百分数）收敛为小数收益矩阵的深模块——÷100 单位换算、全日期覆盖过滤、净值窗口精确对齐单点实现，生产反推（style_track）/回测（walk_forward）/R1.5 装配（monitor）/特征反推（calculator）消费；R1.5 的板块收益序列另经 sector_return_series 按基金实际净值日对齐。
_Avoid_: 板块涨幅、行业收益序列

**统计原语**（features/stats）：
features 域内的秩相关/线性相关/显著性检验命名函数（内部 scipy，ADR-0007 豁免边界内）；引擎层只消费命名函数、不直接 import scipy（test_stats_primitives 守边界）。
_Avoid_: scipy 直连

**面板采样**（panel_samples）：
生产训练（prepare_training_data）与研究回测（backtest_model）共用的样本构建深函数：净值→预热→步进采样→特征现算（含 style_r2 反推与市场状态注入）→多标签（实际收益 y_abs + λ 风险调整标签集）。
_Avoid_: 采样循环拷贝

**期望交易日**：
今日在交易日历内→昨交易日（盘前拉 T-1），否则最近交易日；收敛于 trading_calendar.expected_trade_date，指数/特征新鲜度共用。
_Avoid_: 期望日期（多实现）