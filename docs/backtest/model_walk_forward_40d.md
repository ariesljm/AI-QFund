# 模型级 walk-forward 回测报告（A3 style_r2 增益 + B λ 标定）

- 日期：2026-09-12
- 引擎：`app/engine/backtest_model.py`（`python -m app.engine.backtest_model 1200`）
- 样本：1200 基金 × 决策日步 20 = **13440 条**，验证集 2688 条（最新 20%，2026-04-29 ~ 2026-07-09）
- 前瞻：40 交易日；特征与生产训练同口径（现算含 style_r2 + 市场状态注入 + 时间衰减权重）

## A3：style_r2 进特征对 40 日赚钱胜率的影响（λ=0.5 标签）

| 特征集 | 维度 | 高分组胜率 | 低分组胜率 | 胜率差 | IC |
|---|---|---|---|---|---|
| 含 style_r2 | 19 | 36.4% | 36.0% | +0.4pp | -0.022 |
| 不含 style_r2 | 18 | 36.9% | 35.5% | +1.4pp | -0.003 |

**解读**：验证期（2026-04~07）为单边下跌段（整体 40 日胜率仅 ~36%），模型区分度整体失效（两模型 IC 均≈0）。
模型级**未复现** P5 单因子回归的 r2 增益（+0.045, p<0.001，全样本 2024-06~2026-07）。
含/不含差异在噪声内。**结论：保留 style_r2**（P5 全样本证据强；模型级不显著但无害）。

## B：λ 标定（含 style_r2 特征）

| λ | 高分组胜率 | 低分组胜率 | 胜率差 | IC |
|---|---|---|---|---|
| 0.3 | 35.9% | 36.5% | -0.7pp | -0.066 |
| 0.5（旧生产值） | 36.4% | 36.0% | +0.4pp | -0.022 |
| 0.7 | 37.3% | 35.1% | +2.2pp | +0.014 |
| 1.0 | 37.9% | 34.5% | +3.3pp | **+0.075** |

**解读**：0.3→1.0 **单调改善**（胜率差 -0.7 → +3.3pp，IC -0.066 → +0.075）。
λ=1.0 为测试上界且最优 → **RISK_ADJ_DD_LAMBDA 0.5 → 1.0**，标签版本 v1 → v2，已重训。

## 局限与遗留

- 验证期单一（单边下跌段），λ 单调趋势可能继续外推（>1.0 未测）；1.0 已是合理风险厌恶上界
- A3 的模型级验证受验证段限制，P5 全样本单因子证据仍为 style_r2 保留依据
- 后续：更长期限验证窗口（如 24 个月）重跑一次实验确认 λ=1.0 稳定性
