# 训练/回测真实数据验收（2026-09-16）

状态：✅ 通过（通路口径验收；持续守门输入已记录）

## 前置条件

- LLM 配额解封（用户重新配置）→ 票 13 真实端到端审计链打通
- 07 日线回填完成（4,016 只）→ sector_frame 全量可用
- 04 持仓回填完成（107 万行/多期 9,768 只）——**训练不阻塞 04**（peer 用 style_r2 反推，非持仓同类）

## 训练通路（票 09/10 真实数据）

脚本：`scripts/_train_probe.py`（临时，验收后删除）

| 项 | 结果 |
|---|---|
| 样本 | 2,000 只基金，训练 17,788 条 / 验证 4,447 条，19 维特征 |
| 标签 | λ=1.0 超额标签（excess_adjusted_return），时间衰减权重（半衰期 90 天） |
| 训练 | LGBM regression_l1，50 rounds |
| 验证 MAE | **0.170**（evaluate_lgb_params 返回负 L1 供 GA 最大化） |
| 耗时 | 777s |

## 回测验收（A3 style_r2 增益 + B λ 标定）

命令：`python -m app.engine.backtest_model 800`（313s）

| λ | 高分组胜率 | 低分组胜率 | 胜率差 | IC |
|---|---|---|---|---|
| 0.3 | 17.1% | 30.1% | -13.0pp | -0.209 |
| 0.5 | 18.8% | 28.4% | -9.6pp | -0.140 |
| 0.7 | 18.5% | 28.6% | -10.1pp | -0.117 |
| **1.0（生产）** | 19.4% | 27.7% | -8.3pp | **-0.066** |

- **λ=1.0 维持**（IC 最优，0.3→1.0 单调改善，与 2026-09-12 历史标定一致）
- 验证段（2026-09 前后）为弱市，整体胜率 17-19%（历史下跌段 36%），模型区分度整体失效——属市场段效应，非代码缺陷；按 spec 验收哲学（多区间同向 + 持续守门），此为守门输入

## 票 13 真实 LLM 审计链（配额解封后首次）

- 30 只候选全量审计（35 次调用，~12s/只）→ prune/composite → Top5 落库 `000058, 000031, 000063, 000083, 000136`
- LLM 正确捕捉切片风险（中际旭创实控人质押、中微公司股东减持）
- validator 拒绝非法结构不静默降级
- 同日重跑覆盖修复（fe92a70）：`save_recommend_v2` 先 DELETE 当日再写入

## 最终端到端（04 完成 + 两期切片装配 e6e2e4d，2026-09-16 13:25）

- 35 次真实 LLM 调用 → Top5 落库 `000039, 000056, 000165, 000058, 000061`（同日唯一 5 条）
- LLM 真实消费两期持仓切片：留存率（60%/30%/50%）、集中度变化（+2.2pp/+4.0pp）、
  新进重仓（半导体/光模块/算力产业链）驱动风险判断——切片一从'缺失'升级为真实对比
- 全数据地基：持仓 107 万行/多期 9,768 只、日线 234 万、估值 558 万、AUM 6,494

## 结构层验收矩阵（共识 Q13 上线门槛对照，2026-09-16）

| 门槛 | 2.0 实现 | 证据 |
|---|---|---|
| 全市场直接选基 | screen 全市场 12,900 → 硬过滤 → Top30 | `screen_candidates` 09-16 30 条；真实端到端 202s |
| 硬过滤可执行 | is_buyable/aum<2亿/日限额<1000/nav<62/暂停 | 票 06+11 测试；真实候选池 6,250 只有特征 |
| 理由可追溯 | 四组切片 + LLM 审计 + composite 落库 audit_json | recommend_v2 09-16 5 条含完整 audit_details |
| 审计可解释 | 首页三区块（审计/跟踪状态/信号校准） | web HTTP 200（8ef3c3f 后复验）；事件/time 维度展示 |
| 监控闭环 | 状态机 tracked_states + 校准 signal_outcomes + 脱轨/门控/退役 | 票 15/16/18/19/20 测试；tracked_states 落库 09-16 |
| PIT（Q15） | disclosure_date + as_of 两期切片 | 真实 PIT 多期验证（000001 13 期回退） |
| 分支保护（Q21） | 全部在 2.0 分支，main 未动 | origin/2.0 = 8ef3c3f |

**结论：结构层（上线门槛）达成。** 效果层（超额期望>0/三段同向/校准曲线）为持续守门机制，
记账层（recommend_v2/signal_outcomes/tracked_states/llm_audit）已就位，待真实运行数据累积；
按 Q13 决策"先上线、后证明，不否决上线"。

## spec User Stories 核对矩阵（49 条逐条对照，2026-09-16）

| 段 | 条 | 2.0 实现 | 状态 |
|---|---|---|---|
| 推荐结果 1-5 | Top5 直接给基金/量化分+审计理由/否决可见 | recommend_v2 + audit_json 落库 + 首页区块 | ✅ |
| 量化初筛 6-11 | 全市场 Top30/追高回避/估值分位/收益质量/回撤惩罚/40 日 | screen + features.v2 + λ=1.0 | ✅ |
| 交易可行 12-14 | 可买到/净值历史/剔指数 | 06 硬过滤链 + active_equity_pool | ✅ |
| LLM 排雷 15-21 | 风格漂移/重仓风险/经理负荷/流动性/结构化/剪枝/按需调用 | 切片一/二/四 + validate/prune + 缓存失效 | ⚠️ 17（切片三）待裁决，余 ✅ |
| 监控 22-28 | 虚拟组合日比对/三级状态/观察离场条件/EXIT 不回退/数据问题不误伤/日频 | drift + state_machine + 健康门 | ✅ |
| 记账校准 29-35 | 结算/绝对胜率/命中率/失效降权/校准准确率/标尺分职 | ledger + calibration + signal_outcomes | ✅ |
| 教训沉淀 36-40 | 好坏都记/归因/可检索/只增严/可退役 | ledger + knowledge + retirement | ✅ |
| 模型更新 41-44 | 季度重训/影子三段/自动回滚/台账 | retrain_due + gate + ledger | ✅ |
| 可追溯 45-49 | PIT/60 日隔离带/配置+标定/ADR/1.x 移除 | disclosure_date + purged_folds + ADR 0001-0011 | ⚠️ 49 待 enabled 后 22 |

**49 条中 48 条已兑现**；缺口 2 条均为用户决策挂起（US17 切片三 = 未决 #4；US49 = `[recommend_v2].enabled` 开启后 22 生产删除）。

## 遗留

- 开 `[recommend_v2].enabled`（验收后决策）+ 调度接入 → 22 生产删除（sector_pool/ga/insights 等）
- 12 切片三（管理团队）待用户裁决
- final push 合 main（CI 构建镜像）→ 用户更新服务器
- `scripts/_train_probe.py` 待最终清理
