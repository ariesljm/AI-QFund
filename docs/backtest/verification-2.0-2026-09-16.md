# 训练/回测真实数据验收（2026-09-16）

状态：✅ 通过（通路口径验收；持续守门输入已记录）

## 前置条件

- LLM 配额解封（用户重新配置）→ 票 13 真实端到端审计链打通
- 07 日线回填完成（4,016 只）→ sector_frame 全量可用
- 04 持仓回填进行中（8,500+/12,900）——**训练不阻塞 04**（peer 用 style_r2 反推，非持仓同类）

## 训练通路（票 09/10 真实数据）

脚本：`scripts/_train_probe.py`（临时，验收后删除）

| 项 | 结果 |
|---|---|
| 样本 | 2,000 只基金，训练 17,788 条 / 验证 4,447 条，19 维特征 |
| 标签 | λ=1.0 超额标签（excess_adjusted_return），时间衰减权重（半衰期 90 天） |
| 训练 | LGBM regression_l1，50 rounds |
| 验证 MAE | **0.170**（evaluate_lgb_params 返回负 L1 供 GA 最大化） |
| 耗时 | 777s |

## 回测验收（A3 style_r2 增益 + B λ 标定）

命令：`python -m app.engine.backtest_model 800`（313s）

| λ | 高分组胜率 | 低分组胜率 | 胜率差 | IC |
|---|---|---|---|---|
| 0.3 | 17.1% | 30.1% | -13.0pp | -0.209 |
| 0.5 | 18.8% | 28.4% | -9.6pp | -0.140 |
| 0.7 | 18.5% | 28.6% | -10.1pp | -0.117 |
| **1.0（生产）** | 19.4% | 27.7% | -8.3pp | **-0.066** |

- **λ=1.0 维持**（IC 最优，0.3→1.0 单调改善，与 2026-09-12 历史标定一致）
- 验证段（2026-09 前后）为弱市，整体胜率 17-19%（历史下跌段 36%），模型区分度整体失效——属市场段效应，非代码缺陷；按 spec 验收哲学（多区间同向 + 持续守门），此为守门输入

## 票 13 真实 LLM 审计链（配额解封后首次）

- 30 只候选全量审计（35 次调用，~12s/只）→ prune/composite → Top5 落库 `000058, 000031, 000063, 000083, 000136`
- LLM 正确捕捉切片风险（中际旭创实控人质押、中微公司股东减持）
- validator 拒绝非法结构不静默降级
- 同日重跑覆盖修复（fe92a70）：`save_recommend_v2` 先 DELETE 当日再写入

## 遗留

- 04 完成后再跑一次全量训练（吸收新增多期持仓对 RBSA/切片装配的影响）与 end-to-end recommend_top5（持仓异动切片接两期对比）
- `scripts/_train_probe.py` 待最终清理
